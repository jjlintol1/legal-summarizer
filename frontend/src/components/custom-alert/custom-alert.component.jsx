import React from 'react';

import { Alert } from '@mui/material'

const CustomAlert = ({ severity, open, message, onClose }) => {
  return (
    <Alert>

    </Alert>
  )
}

export default CustomAlert