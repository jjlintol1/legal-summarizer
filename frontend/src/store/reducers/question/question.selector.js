export const selectActiveDocument = (state) => state.question.activeDocument;

export const selectQuestions = (state) => state.question.questions;

export const selectQuestionsIsLoading = (state) => state.question.isLoading;